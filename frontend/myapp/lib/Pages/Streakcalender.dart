import 'package:flutter/material.dart';

class Streakcalender extends StatelessWidget {
  const Streakcalender({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("Streakcalender"),
    );
  }
}
